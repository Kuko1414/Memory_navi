我觉得这个模式可以叫执行模式，也就是从区域快速到达另一区域的内容。

首先呢是我们要先有supervisor，此前我们还没做这个，supervisor需要通过词向量检测任务处于哪个阶段（无语义记忆-探索模式， 有指定区域但无任务物体-补全模式， 有区域和任务物体记忆信息-执行模式），然后把封装好的skill和对应tool发送给qwen和claude。

claude需要先读取记忆，得到 qwen所在区域的入口-以这个入口导航到可以执行区域的语义拓扑信息（比如：东工作区门口往右走，看到红色柜子往左走，然后西工作区入口位于西方向两个红色矮柜子的中央）-发送目标区域所在的入口中心- 再导航到所需要的地方 （我们前期观测，可以做的简单一点，比如靠近某个物体，距离它0.1m以内这样）

claude任务：读取记忆，解读这个任务流程（当前门口-拓扑信息到任务门口-靠近物体）发送给qwen

qwen：依靠记忆信息和claude提供的任务解读来完成任务，强调任务路线如何执行：
比如拿到任务
    - 先读取当前位姿和所要离开的房间的入口，通过scan和depth判断第一步动作
    - 利用VFH导航到入口，再次利用scan和depth判断入口出情况，结合claude提到的参照物，规划合理的路线，利用VFH导航过去
    - 每次移动之后，应该先探查scan和rgb得知附近环境，尤其是走到任务房间入口附近时，保证进入入口时的视野是开阔的
    - 到达入口后，调用该区域的语义记忆，得知哪里可通行，哪里不能走，定位到物体所在位置后，应该保持尽量面向物体的位姿靠近，到达后发布finish指令
    - 发布finish的同时，应该附带一段简单的任务总结，配合finish指令传回给supervisor

预想此次任务需要：
    - 封装好之前的探索模式/补全模式，它们如何执行该模式的skill，prompt，所用到的工具，对记忆的利用方式，和claude需要做的任务都应清晰的声明，每个模式相互独立，不要数据污染
    - 搭建supervisor的代码（supervisor的任务可以在report中找到），根据词向量判定任务方式，给claude和qwen输入对应的模式，分发任务，并检测qwen最终是否有发finish，附带qwen的总结和任务时间，时长等简要信息（最终回报应该是每个模式都该有的）
    - 开始实现新的执行模式，利用理想的全局语义地图做初期实验（因为探索模式的幻觉严重导致用户在评估微调的价值）
    - 简单的评判（检测有几次碰撞，finish任务的时间，是否真的完成了任务（检查任务物体和小车距离是否小于0.1,记得考虑小车本身体积问题），qwen是否输出总结，supervisor是否回报qwen的信息），实现完整信息和任务双闭环流程
    - 此次任务为初期实验，不论结果如何，就算有bug，也要先停下评估可行性/bug问题/是否有可优化内容。并把执行任务期间的数据和图片存储存到一个文件夹内，放到/Report里。
    - 如果你对该任务的流程或某些具体细节不太清楚，一定要向用户询问！就算用户的想法不合适，也要提出反驳，直到双方认可某项技术的落地足够合理。
